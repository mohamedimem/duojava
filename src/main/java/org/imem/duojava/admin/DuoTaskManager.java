package org.imem.duojava.admin;

import org.imem.duojava.Section.task.model.Task;
import org.imem.duojava.Section.task.model.TaskType;

public class DuoTaskManager extends Task {

    public DuoTaskManager(int id, String name, TaskType type) {
        super(id, name, type);
    }

    public  void create(){
        //get data input
        //convert input to json
        //save json in local

        // Convert input data to JSON

    }
}
